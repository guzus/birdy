interface:
  display_name: "Birdy"
  short_description: "Install and operate birdy (multi-account bird proxy)"
  default_prompt: "Use $birdy to ensure the birdy CLI is installed, then configure accounts/rotation, run bird commands, and troubleshoot bird detection and Node requirements."
